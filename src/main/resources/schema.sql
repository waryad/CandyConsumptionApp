CREATE TABLE halloweencandyconsumption (
    id INT PRIMARY KEY AUTO_INCREMENT,
    quantity VARCHAR(255),
    candyBrandname VARCHAR(255),
    beforeDate DATE,
    description VARCHAR(255),
    category VARCHAR(255)
);